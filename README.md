# Backstabbr Extras — Android

A minimal Android app that wraps [backstabbr.com](https://www.backstabbr.com) in a
`WebView` and injects the **Backstabbr Extras** Chrome-extension content scripts into
the page.


So yea basically the backstabbr extension into an apk. :)