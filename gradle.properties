org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.caching=true
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# Path to the Chrome-extension repo used as the single source of truth for the
# injected JS/CSS. Overridable: -Pbse.extensionDir=C:/path/to/backstabbr-extras
# Default assumes it sits next to this project inside Documents.
bse.extensionDir=../backstabbr-extras
